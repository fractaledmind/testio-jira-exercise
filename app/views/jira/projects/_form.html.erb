<%= form_with(model: jira_project, local: true) do |form| %>
  <% if jira_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jira_project.errors.count, "error") %> prohibited this jira_project from being saved:</h2>

      <ul>
      <% jira_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
