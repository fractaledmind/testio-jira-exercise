<%= form_with(model: jira_issue, local: true) do |form| %>
  <% if jira_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jira_issue.errors.count, "error") %> prohibited this jira_issue from being saved:</h2>

      <ul>
      <% jira_issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :jira_project_id %>
    <%= form.text_field :jira_project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
