<%= form_with(model: jira_user, local: true) do |form| %>
  <% if jira_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jira_user.errors.count, "error") %> prohibited this jira_user from being saved:</h2>

      <ul>
      <% jira_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
